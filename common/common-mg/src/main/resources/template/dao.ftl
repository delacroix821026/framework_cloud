package ${package};

import ${baseModelPackage}.${domainName};
import ${baseModelPackage}.${domainName}Example;
import ${genericServicePackage};

public interface ${domainName}Dao extends GenericService<${domainName}, ${domainName}Example, ${primaryKeyType}> {

}
